type: object
description: Status denotes the overall status of the result
required:
  - code
properties:
  code:
    type: string
    description: The status code
    enum:
      - ok
      - error
      - empty
    example: ok
  message:
    type: string
    description: An optional message
    example: "Query succeeded"
