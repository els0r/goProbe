package globalquery

const (

	// QueryRoute denotes the route / URI path to the query endpoint
	QueryRoute = "/_query"
	// ValidationRoute denotes the route / URI path to validate query arguments
	ValidationRoute = QueryRoute + "/validate"
)
