get:
  summary: Get interface statistics
  tags:
    - control
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: '../schemas/StatusResponse.yaml'
